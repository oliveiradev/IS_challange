<div class="container">
    <div class="row">
        <div class="col-md-7">
            <h1><%= t('show.course') %></h1>
            <table class="table table-striped">
                <tr>
                    <th><%= t('course.name') %></th>
                    <td>
                        <%=@course.name%>
                    </td>
                </tr>
                <tr>
                    <th><%= t('course.description') %></th>
                    <td>
                        <%=@course.description%>
                    </td>
                </tr>
            </table>
        </div>
    </div>
</div>